defmodule Nspark.Repo.Migrations.AddSkillActorTracking do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:skills) do
      add :created_by_id,
          references(:users,
            column: :id,
            name: "skills_created_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :updated_by_id,
          references(:users,
            column: :id,
            name: "skills_updated_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end
  end

  def down do
    drop constraint(:skills, "skills_created_by_id_fkey")

    drop constraint(:skills, "skills_updated_by_id_fkey")

    alter table(:skills) do
      remove :updated_by_id
      remove :created_by_id
    end
  end
end
